import 'package:flutter/material.dart';


class Procedure extends StatelessWidget {
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
            appBar: AppBar(title: Text('Prosedur'),),
            body:Text('') ,
        ));
  }
}
