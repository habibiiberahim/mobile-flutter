import 'package:flutter/material.dart';


class Evacuation extends StatelessWidget {
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
            appBar: AppBar(title:Text('Evakuasi')),
            body:Text('') ,
        ));
  }
}
