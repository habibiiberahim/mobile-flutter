import 'package:flutter/material.dart';


class MapStation extends StatelessWidget {
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
            appBar: AppBar(title:Text('Denah'),),
            body:Text('') ,
        ));
  }
}
